# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PriceBasisSwapRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PriceBasisSwapRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPriceBasisSwapRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PriceBasisSwapRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PriceBasisSwapRequest
    def Pricing(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from quantra.Pricing import Pricing
            obj = Pricing()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PriceBasisSwapRequest
    def Swaps(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from quantra.PriceBasisSwap import PriceBasisSwap
            obj = PriceBasisSwap()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PriceBasisSwapRequest
    def SwapsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PriceBasisSwapRequest
    def SwapsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # PriceBasisSwapRequest
    def IncludeFlows(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def PriceBasisSwapRequestStart(builder):
    builder.StartObject(3)

def Start(builder):
    PriceBasisSwapRequestStart(builder)

def PriceBasisSwapRequestAddPricing(builder, pricing):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(pricing), 0)

def AddPricing(builder, pricing):
    PriceBasisSwapRequestAddPricing(builder, pricing)

def PriceBasisSwapRequestAddSwaps(builder, swaps):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(swaps), 0)

def AddSwaps(builder, swaps):
    PriceBasisSwapRequestAddSwaps(builder, swaps)

def PriceBasisSwapRequestStartSwapsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSwapsVector(builder, numElems):
    return PriceBasisSwapRequestStartSwapsVector(builder, numElems)

def PriceBasisSwapRequestAddIncludeFlows(builder, includeFlows):
    builder.PrependBoolSlot(2, includeFlows, 0)

def AddIncludeFlows(builder, includeFlows):
    PriceBasisSwapRequestAddIncludeFlows(builder, includeFlows)

def PriceBasisSwapRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return PriceBasisSwapRequestEnd(builder)

try:
    from typing import List, Optional
except:
    pass

class PriceBasisSwapRequestT(object):

    # PriceBasisSwapRequestT
    def __init__(self):
        self.pricing = None  # type: Optional[PricingT]
        self.swaps = None  # type: List[PriceBasisSwapT]
        self.includeFlows = False  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        priceBasisSwapRequest = PriceBasisSwapRequest()
        priceBasisSwapRequest.Init(buf, pos)
        return cls.InitFromObj(priceBasisSwapRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, priceBasisSwapRequest):
        x = PriceBasisSwapRequestT()
        x._UnPack(priceBasisSwapRequest)
        return x

    # PriceBasisSwapRequestT
    def _UnPack(self, priceBasisSwapRequest):
        if priceBasisSwapRequest is None:
            return
        if priceBasisSwapRequest.Pricing() is not None:
            self.pricing = PricingT.InitFromObj(priceBasisSwapRequest.Pricing())
        if not priceBasisSwapRequest.SwapsIsNone():
            self.swaps = []
            for i in range(priceBasisSwapRequest.SwapsLength()):
                if priceBasisSwapRequest.Swaps(i) is None:
                    self.swaps.append(None)
                else:
                    priceBasisSwap_ = PriceBasisSwapT.InitFromObj(priceBasisSwapRequest.Swaps(i))
                    self.swaps.append(priceBasisSwap_)
        self.includeFlows = priceBasisSwapRequest.IncludeFlows()

    # PriceBasisSwapRequestT
    def Pack(self, builder):
        if self.pricing is not None:
            pricing = self.pricing.Pack(builder)
        if self.swaps is not None:
            swapslist = []
            for i in range(len(self.swaps)):
                swapslist.append(self.swaps[i].Pack(builder))
            PriceBasisSwapRequestStartSwapsVector(builder, len(self.swaps))
            for i in reversed(range(len(self.swaps))):
                builder.PrependUOffsetTRelative(swapslist[i])
            swaps = builder.EndVector()
        PriceBasisSwapRequestStart(builder)
        if self.pricing is not None:
            PriceBasisSwapRequestAddPricing(builder, pricing)
        if self.swaps is not None:
            PriceBasisSwapRequestAddSwaps(builder, swaps)
        PriceBasisSwapRequestAddIncludeFlows(builder, self.includeFlows)
        priceBasisSwapRequest = PriceBasisSwapRequestEnd(builder)
        return priceBasisSwapRequest
