# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Credit market data grouped under the shared Pricing context.
class CreditMarketData(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CreditMarketData()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCreditMarketData(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # CreditMarketData
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Credit curves for CDS pricing.
    # CreditMarketData
    def CreditCurves(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from quantra.CreditCurveSpec import CreditCurveSpec
            obj = CreditCurveSpec()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CreditMarketData
    def CreditCurvesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CreditMarketData
    def CreditCurvesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def CreditMarketDataStart(builder):
    builder.StartObject(1)

def Start(builder):
    CreditMarketDataStart(builder)

def CreditMarketDataAddCreditCurves(builder, creditCurves):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(creditCurves), 0)

def AddCreditCurves(builder, creditCurves):
    CreditMarketDataAddCreditCurves(builder, creditCurves)

def CreditMarketDataStartCreditCurvesVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCreditCurvesVector(builder, numElems):
    return CreditMarketDataStartCreditCurvesVector(builder, numElems)

def CreditMarketDataEnd(builder):
    return builder.EndObject()

def End(builder):
    return CreditMarketDataEnd(builder)

try:
    from typing import List
except:
    pass

class CreditMarketDataT(object):

    # CreditMarketDataT
    def __init__(self):
        self.creditCurves = None  # type: List[CreditCurveSpecT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        creditMarketData = CreditMarketData()
        creditMarketData.Init(buf, pos)
        return cls.InitFromObj(creditMarketData)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, creditMarketData):
        x = CreditMarketDataT()
        x._UnPack(creditMarketData)
        return x

    # CreditMarketDataT
    def _UnPack(self, creditMarketData):
        if creditMarketData is None:
            return
        if not creditMarketData.CreditCurvesIsNone():
            self.creditCurves = []
            for i in range(creditMarketData.CreditCurvesLength()):
                if creditMarketData.CreditCurves(i) is None:
                    self.creditCurves.append(None)
                else:
                    creditCurveSpec_ = CreditCurveSpecT.InitFromObj(creditMarketData.CreditCurves(i))
                    self.creditCurves.append(creditCurveSpec_)

    # CreditMarketDataT
    def Pack(self, builder):
        if self.creditCurves is not None:
            creditCurveslist = []
            for i in range(len(self.creditCurves)):
                creditCurveslist.append(self.creditCurves[i].Pack(builder))
            CreditMarketDataStartCreditCurvesVector(builder, len(self.creditCurves))
            for i in reversed(range(len(self.creditCurves))):
                builder.PrependUOffsetTRelative(creditCurveslist[i])
            creditCurves = builder.EndVector()
        CreditMarketDataStart(builder)
        if self.creditCurves is not None:
            CreditMarketDataAddCreditCurves(builder, creditCurves)
        creditMarketData = CreditMarketDataEnd(builder)
        return creditMarketData
