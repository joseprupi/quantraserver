# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PriceVanillaSwapRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PriceVanillaSwapRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPriceVanillaSwapRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PriceVanillaSwapRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PriceVanillaSwapRequest
    def Pricing(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = Pricing()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PriceVanillaSwapRequest
    def Swaps(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = PriceVanillaSwap()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PriceVanillaSwapRequest
    def SwapsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PriceVanillaSwapRequest
    def SwapsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # PriceVanillaSwapRequest
    def IncludeFlows(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def PriceVanillaSwapRequestStart(builder):
    builder.StartObject(3)

def Start(builder):
    PriceVanillaSwapRequestStart(builder)

def PriceVanillaSwapRequestAddPricing(builder, pricing):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(pricing), 0)

def AddPricing(builder, pricing):
    PriceVanillaSwapRequestAddPricing(builder, pricing)

def PriceVanillaSwapRequestAddSwaps(builder, swaps):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(swaps), 0)

def AddSwaps(builder, swaps):
    PriceVanillaSwapRequestAddSwaps(builder, swaps)

def PriceVanillaSwapRequestStartSwapsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSwapsVector(builder, numElems):
    return PriceVanillaSwapRequestStartSwapsVector(builder, numElems)

def PriceVanillaSwapRequestAddIncludeFlows(builder, includeFlows):
    builder.PrependBoolSlot(2, includeFlows, 0)

def AddIncludeFlows(builder, includeFlows):
    PriceVanillaSwapRequestAddIncludeFlows(builder, includeFlows)

def PriceVanillaSwapRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return PriceVanillaSwapRequestEnd(builder)

try:
    from typing import List, Optional
except:
    pass

class PriceVanillaSwapRequestT(object):

    # PriceVanillaSwapRequestT
    def __init__(self):
        self.pricing = None  # type: Optional[PricingT]
        self.swaps = None  # type: List[PriceVanillaSwapT]
        self.includeFlows = False  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        priceVanillaSwapRequest = PriceVanillaSwapRequest()
        priceVanillaSwapRequest.Init(buf, pos)
        return cls.InitFromObj(priceVanillaSwapRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, priceVanillaSwapRequest):
        x = PriceVanillaSwapRequestT()
        x._UnPack(priceVanillaSwapRequest)
        return x

    # PriceVanillaSwapRequestT
    def _UnPack(self, priceVanillaSwapRequest):
        if priceVanillaSwapRequest is None:
            return
        if priceVanillaSwapRequest.Pricing() is not None:
            self.pricing = PricingT.InitFromObj(priceVanillaSwapRequest.Pricing())
        if not priceVanillaSwapRequest.SwapsIsNone():
            self.swaps = []
            for i in range(priceVanillaSwapRequest.SwapsLength()):
                if priceVanillaSwapRequest.Swaps(i) is None:
                    self.swaps.append(None)
                else:
                    priceVanillaSwap_ = PriceVanillaSwapT.InitFromObj(priceVanillaSwapRequest.Swaps(i))
                    self.swaps.append(priceVanillaSwap_)
        self.includeFlows = priceVanillaSwapRequest.IncludeFlows()

    # PriceVanillaSwapRequestT
    def Pack(self, builder):
        if self.pricing is not None:
            pricing = self.pricing.Pack(builder)
        if self.swaps is not None:
            swapslist = []
            for i in range(len(self.swaps)):
                swapslist.append(self.swaps[i].Pack(builder))
            PriceVanillaSwapRequestStartSwapsVector(builder, len(self.swaps))
            for i in reversed(range(len(self.swaps))):
                builder.PrependUOffsetTRelative(swapslist[i])
            swaps = builder.EndVector()
        PriceVanillaSwapRequestStart(builder)
        if self.pricing is not None:
            PriceVanillaSwapRequestAddPricing(builder, pricing)
        if self.swaps is not None:
            PriceVanillaSwapRequestAddSwaps(builder, swaps)
        PriceVanillaSwapRequestAddIncludeFlows(builder, self.includeFlows)
        priceVanillaSwapRequest = PriceVanillaSwapRequestEnd(builder)
        return priceVanillaSwapRequest
