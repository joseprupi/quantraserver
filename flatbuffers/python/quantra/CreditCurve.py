# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class CreditCurve(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CreditCurve()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCreditCurve(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # CreditCurve
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CreditCurve
    def RecoveryRate(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.4

    # CreditCurve
    def Quotes(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from quantra.CreditSpreadQuote import CreditSpreadQuote
            obj = CreditSpreadQuote()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CreditCurve
    def QuotesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CreditCurve
    def QuotesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # CreditCurve
    def FlatHazardRate(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def CreditCurveStart(builder):
    builder.StartObject(3)

def Start(builder):
    CreditCurveStart(builder)

def CreditCurveAddRecoveryRate(builder, recoveryRate):
    builder.PrependFloat64Slot(0, recoveryRate, 0.4)

def AddRecoveryRate(builder, recoveryRate):
    CreditCurveAddRecoveryRate(builder, recoveryRate)

def CreditCurveAddQuotes(builder, quotes):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(quotes), 0)

def AddQuotes(builder, quotes):
    CreditCurveAddQuotes(builder, quotes)

def CreditCurveStartQuotesVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartQuotesVector(builder, numElems):
    return CreditCurveStartQuotesVector(builder, numElems)

def CreditCurveAddFlatHazardRate(builder, flatHazardRate):
    builder.PrependFloat64Slot(2, flatHazardRate, 0.0)

def AddFlatHazardRate(builder, flatHazardRate):
    CreditCurveAddFlatHazardRate(builder, flatHazardRate)

def CreditCurveEnd(builder):
    return builder.EndObject()

def End(builder):
    return CreditCurveEnd(builder)

try:
    from typing import List
except:
    pass

class CreditCurveT(object):

    # CreditCurveT
    def __init__(self):
        self.recoveryRate = 0.4  # type: float
        self.quotes = None  # type: List[CreditSpreadQuoteT]
        self.flatHazardRate = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        creditCurve = CreditCurve()
        creditCurve.Init(buf, pos)
        return cls.InitFromObj(creditCurve)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, creditCurve):
        x = CreditCurveT()
        x._UnPack(creditCurve)
        return x

    # CreditCurveT
    def _UnPack(self, creditCurve):
        if creditCurve is None:
            return
        self.recoveryRate = creditCurve.RecoveryRate()
        if not creditCurve.QuotesIsNone():
            self.quotes = []
            for i in range(creditCurve.QuotesLength()):
                if creditCurve.Quotes(i) is None:
                    self.quotes.append(None)
                else:
                    creditSpreadQuote_ = CreditSpreadQuoteT.InitFromObj(creditCurve.Quotes(i))
                    self.quotes.append(creditSpreadQuote_)
        self.flatHazardRate = creditCurve.FlatHazardRate()

    # CreditCurveT
    def Pack(self, builder):
        if self.quotes is not None:
            quoteslist = []
            for i in range(len(self.quotes)):
                quoteslist.append(self.quotes[i].Pack(builder))
            CreditCurveStartQuotesVector(builder, len(self.quotes))
            for i in reversed(range(len(self.quotes))):
                builder.PrependUOffsetTRelative(quoteslist[i])
            quotes = builder.EndVector()
        CreditCurveStart(builder)
        CreditCurveAddRecoveryRate(builder, self.recoveryRate)
        if self.quotes is not None:
            CreditCurveAddQuotes(builder, quotes)
        CreditCurveAddFlatHazardRate(builder, self.flatHazardRate)
        creditCurve = CreditCurveEnd(builder)
        return creditCurve
