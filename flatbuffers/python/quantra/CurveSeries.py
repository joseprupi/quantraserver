# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One series aligned to grid_dates.
class CurveSeries(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CurveSeries()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCurveSeries(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # CurveSeries
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Which measure this series represents.
    # CurveSeries
    def Measure(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # Values aligned to grid_dates (same length).
    # CurveSeries
    def Values(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # CurveSeries
    def ValuesAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # CurveSeries
    def ValuesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CurveSeries
    def ValuesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def CurveSeriesStart(builder):
    builder.StartObject(2)

def Start(builder):
    CurveSeriesStart(builder)

def CurveSeriesAddMeasure(builder, measure):
    builder.PrependInt8Slot(0, measure, 0)

def AddMeasure(builder, measure):
    CurveSeriesAddMeasure(builder, measure)

def CurveSeriesAddValues(builder, values):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(values), 0)

def AddValues(builder, values):
    CurveSeriesAddValues(builder, values)

def CurveSeriesStartValuesVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartValuesVector(builder, numElems):
    return CurveSeriesStartValuesVector(builder, numElems)

def CurveSeriesEnd(builder):
    return builder.EndObject()

def End(builder):
    return CurveSeriesEnd(builder)

try:
    from typing import List
except:
    pass

class CurveSeriesT(object):

    # CurveSeriesT
    def __init__(self):
        self.measure = 0  # type: int
        self.values = None  # type: List[float]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        curveSeries = CurveSeries()
        curveSeries.Init(buf, pos)
        return cls.InitFromObj(curveSeries)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, curveSeries):
        x = CurveSeriesT()
        x._UnPack(curveSeries)
        return x

    # CurveSeriesT
    def _UnPack(self, curveSeries):
        if curveSeries is None:
            return
        self.measure = curveSeries.Measure()
        if not curveSeries.ValuesIsNone():
            if np is None:
                self.values = []
                for i in range(curveSeries.ValuesLength()):
                    self.values.append(curveSeries.Values(i))
            else:
                self.values = curveSeries.ValuesAsNumpy()

    # CurveSeriesT
    def Pack(self, builder):
        if self.values is not None:
            if np is not None and type(self.values) is np.ndarray:
                values = builder.CreateNumpyVector(self.values)
            else:
                CurveSeriesStartValuesVector(builder, len(self.values))
                for i in reversed(range(len(self.values))):
                    builder.PrependFloat64(self.values[i])
                values = builder.EndVector()
        CurveSeriesStart(builder)
        CurveSeriesAddMeasure(builder, self.measure)
        if self.values is not None:
            CurveSeriesAddValues(builder, values)
        curveSeries = CurveSeriesEnd(builder)
        return curveSeries
