# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PriceCDSResponse(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PriceCDSResponse()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPriceCDSResponse(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PriceCDSResponse
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PriceCDSResponse
    def CdsList(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from quantra.CDSValues import CDSValues
            obj = CDSValues()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PriceCDSResponse
    def CdsListLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PriceCDSResponse
    def CdsListIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def PriceCDSResponseStart(builder):
    builder.StartObject(1)

def Start(builder):
    PriceCDSResponseStart(builder)

def PriceCDSResponseAddCdsList(builder, cdsList):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(cdsList), 0)

def AddCdsList(builder, cdsList):
    PriceCDSResponseAddCdsList(builder, cdsList)

def PriceCDSResponseStartCdsListVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCdsListVector(builder, numElems):
    return PriceCDSResponseStartCdsListVector(builder, numElems)

def PriceCDSResponseEnd(builder):
    return builder.EndObject()

def End(builder):
    return PriceCDSResponseEnd(builder)

try:
    from typing import List
except:
    pass

class PriceCDSResponseT(object):

    # PriceCDSResponseT
    def __init__(self):
        self.cdsList = None  # type: List[CDSValuesT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        priceCdsresponse = PriceCDSResponse()
        priceCdsresponse.Init(buf, pos)
        return cls.InitFromObj(priceCdsresponse)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, priceCdsresponse):
        x = PriceCDSResponseT()
        x._UnPack(priceCdsresponse)
        return x

    # PriceCDSResponseT
    def _UnPack(self, priceCdsresponse):
        if priceCdsresponse is None:
            return
        if not priceCdsresponse.CdsListIsNone():
            self.cdsList = []
            for i in range(priceCdsresponse.CdsListLength()):
                if priceCdsresponse.CdsList(i) is None:
                    self.cdsList.append(None)
                else:
                    cDSValues_ = CDSValuesT.InitFromObj(priceCdsresponse.CdsList(i))
                    self.cdsList.append(cDSValues_)

    # PriceCDSResponseT
    def Pack(self, builder):
        if self.cdsList is not None:
            cdsListlist = []
            for i in range(len(self.cdsList)):
                cdsListlist.append(self.cdsList[i].Pack(builder))
            PriceCDSResponseStartCdsListVector(builder, len(self.cdsList))
            for i in reversed(range(len(self.cdsList))):
                builder.PrependUOffsetTRelative(cdsListlist[i])
            cdsList = builder.EndVector()
        PriceCDSResponseStart(builder)
        if self.cdsList is not None:
            PriceCDSResponseAddCdsList(builder, cdsList)
        priceCdsresponse = PriceCDSResponseEnd(builder)
        return priceCdsresponse
