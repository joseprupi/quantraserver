# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Single callable fixed-rate bond pricing context with explicit market-data
# references.
class PriceCallableFixedRateBond(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PriceCallableFixedRateBond()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPriceCallableFixedRateBond(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PriceCallableFixedRateBond
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PriceCallableFixedRateBond
    def CallableFixedRateBond(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from quantra.CallableFixedRateBond import CallableFixedRateBond
            obj = CallableFixedRateBond()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Reference to a curve in Pricing.rates.curves used for discounting and as
    # the term structure carried by the Hull-White model / tree engine.
    # PriceCallableFixedRateBond
    def DiscountingCurve(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Reference to a short-rate model in Pricing.volatility.models by id. Must
    # be a SwaptionModelSpec of model_type HullWhiteLattice — the callable
    # bond reuses the swaption product's model mechanism (including its
    # cross-request Hull-White calibration cache), so param_mode=Explicit uses
    # the model's hw_a/hw_sigma and param_mode=Calibrate calibrates against the
    # referenced swaption vol surface.
    # PriceCallableFixedRateBond
    def Model(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Number of time steps for the TreeCallableFixedRateBondEngine lattice.
    # Absent => server default (kDefaultTreeSteps, ~400). Server-side clamped
    # to [kMinTreeSteps, kMaxTreeSteps] = [1, 2000]; the model's own
    # lattice_steps is unrelated and unused here.
    # PriceCallableFixedRateBond
    def TreeSteps(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return None

def PriceCallableFixedRateBondStart(builder):
    builder.StartObject(4)

def Start(builder):
    PriceCallableFixedRateBondStart(builder)

def PriceCallableFixedRateBondAddCallableFixedRateBond(builder, callableFixedRateBond):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(callableFixedRateBond), 0)

def AddCallableFixedRateBond(builder, callableFixedRateBond):
    PriceCallableFixedRateBondAddCallableFixedRateBond(builder, callableFixedRateBond)

def PriceCallableFixedRateBondAddDiscountingCurve(builder, discountingCurve):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(discountingCurve), 0)

def AddDiscountingCurve(builder, discountingCurve):
    PriceCallableFixedRateBondAddDiscountingCurve(builder, discountingCurve)

def PriceCallableFixedRateBondAddModel(builder, model):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(model), 0)

def AddModel(builder, model):
    PriceCallableFixedRateBondAddModel(builder, model)

def PriceCallableFixedRateBondAddTreeSteps(builder, treeSteps):
    builder.PrependInt32Slot(3, treeSteps, None)

def AddTreeSteps(builder, treeSteps):
    PriceCallableFixedRateBondAddTreeSteps(builder, treeSteps)

def PriceCallableFixedRateBondEnd(builder):
    return builder.EndObject()

def End(builder):
    return PriceCallableFixedRateBondEnd(builder)

try:
    from typing import Optional
except:
    pass

class PriceCallableFixedRateBondT(object):

    # PriceCallableFixedRateBondT
    def __init__(self):
        self.callableFixedRateBond = None  # type: Optional[CallableFixedRateBondT]
        self.discountingCurve = None  # type: str
        self.model = None  # type: str
        self.treeSteps = None  # type: Optional[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        priceCallableFixedRateBond = PriceCallableFixedRateBond()
        priceCallableFixedRateBond.Init(buf, pos)
        return cls.InitFromObj(priceCallableFixedRateBond)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, priceCallableFixedRateBond):
        x = PriceCallableFixedRateBondT()
        x._UnPack(priceCallableFixedRateBond)
        return x

    # PriceCallableFixedRateBondT
    def _UnPack(self, priceCallableFixedRateBond):
        if priceCallableFixedRateBond is None:
            return
        if priceCallableFixedRateBond.CallableFixedRateBond() is not None:
            self.callableFixedRateBond = CallableFixedRateBondT.InitFromObj(priceCallableFixedRateBond.CallableFixedRateBond())
        self.discountingCurve = priceCallableFixedRateBond.DiscountingCurve()
        self.model = priceCallableFixedRateBond.Model()
        self.treeSteps = priceCallableFixedRateBond.TreeSteps()

    # PriceCallableFixedRateBondT
    def Pack(self, builder):
        if self.callableFixedRateBond is not None:
            callableFixedRateBond = self.callableFixedRateBond.Pack(builder)
        if self.discountingCurve is not None:
            discountingCurve = builder.CreateString(self.discountingCurve)
        if self.model is not None:
            model = builder.CreateString(self.model)
        PriceCallableFixedRateBondStart(builder)
        if self.callableFixedRateBond is not None:
            PriceCallableFixedRateBondAddCallableFixedRateBond(builder, callableFixedRateBond)
        if self.discountingCurve is not None:
            PriceCallableFixedRateBondAddDiscountingCurve(builder, discountingCurve)
        if self.model is not None:
            PriceCallableFixedRateBondAddModel(builder, model)
        PriceCallableFixedRateBondAddTreeSteps(builder, self.treeSteps)
        priceCallableFixedRateBond = PriceCallableFixedRateBondEnd(builder)
        return priceCallableFixedRateBond
