# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PriceVanillaSwapResponse(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PriceVanillaSwapResponse()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPriceVanillaSwapResponse(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PriceVanillaSwapResponse
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PriceVanillaSwapResponse
    def Swaps(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from quantra.VanillaSwapResponse import VanillaSwapResponse
            obj = VanillaSwapResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PriceVanillaSwapResponse
    def SwapsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PriceVanillaSwapResponse
    def SwapsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def PriceVanillaSwapResponseStart(builder):
    builder.StartObject(1)

def Start(builder):
    PriceVanillaSwapResponseStart(builder)

def PriceVanillaSwapResponseAddSwaps(builder, swaps):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(swaps), 0)

def AddSwaps(builder, swaps):
    PriceVanillaSwapResponseAddSwaps(builder, swaps)

def PriceVanillaSwapResponseStartSwapsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSwapsVector(builder, numElems):
    return PriceVanillaSwapResponseStartSwapsVector(builder, numElems)

def PriceVanillaSwapResponseEnd(builder):
    return builder.EndObject()

def End(builder):
    return PriceVanillaSwapResponseEnd(builder)

try:
    from typing import List
except:
    pass

class PriceVanillaSwapResponseT(object):

    # PriceVanillaSwapResponseT
    def __init__(self):
        self.swaps = None  # type: List[VanillaSwapResponseT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        priceVanillaSwapResponse = PriceVanillaSwapResponse()
        priceVanillaSwapResponse.Init(buf, pos)
        return cls.InitFromObj(priceVanillaSwapResponse)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, priceVanillaSwapResponse):
        x = PriceVanillaSwapResponseT()
        x._UnPack(priceVanillaSwapResponse)
        return x

    # PriceVanillaSwapResponseT
    def _UnPack(self, priceVanillaSwapResponse):
        if priceVanillaSwapResponse is None:
            return
        if not priceVanillaSwapResponse.SwapsIsNone():
            self.swaps = []
            for i in range(priceVanillaSwapResponse.SwapsLength()):
                if priceVanillaSwapResponse.Swaps(i) is None:
                    self.swaps.append(None)
                else:
                    vanillaSwapResponse_ = VanillaSwapResponseT.InitFromObj(priceVanillaSwapResponse.Swaps(i))
                    self.swaps.append(vanillaSwapResponse_)

    # PriceVanillaSwapResponseT
    def Pack(self, builder):
        if self.swaps is not None:
            swapslist = []
            for i in range(len(self.swaps)):
                swapslist.append(self.swaps[i].Pack(builder))
            PriceVanillaSwapResponseStartSwapsVector(builder, len(self.swaps))
            for i in reversed(range(len(self.swaps))):
                builder.PrependUOffsetTRelative(swapslist[i])
            swaps = builder.EndVector()
        PriceVanillaSwapResponseStart(builder)
        if self.swaps is not None:
            PriceVanillaSwapResponseAddSwaps(builder, swaps)
        priceVanillaSwapResponse = PriceVanillaSwapResponseEnd(builder)
        return priceVanillaSwapResponse
