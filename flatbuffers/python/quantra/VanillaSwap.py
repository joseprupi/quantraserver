# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class VanillaSwap(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VanillaSwap()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVanillaSwap(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # VanillaSwap
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # VanillaSwap
    def SwapType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # VanillaSwap
    def FixedLeg(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from quantra.SwapFixedLeg import SwapFixedLeg
            obj = SwapFixedLeg()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VanillaSwap
    def FloatingLeg(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from quantra.SwapFloatingLeg import SwapFloatingLeg
            obj = SwapFloatingLeg()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def VanillaSwapStart(builder):
    builder.StartObject(3)

def Start(builder):
    VanillaSwapStart(builder)

def VanillaSwapAddSwapType(builder, swapType):
    builder.PrependInt8Slot(0, swapType, 0)

def AddSwapType(builder, swapType):
    VanillaSwapAddSwapType(builder, swapType)

def VanillaSwapAddFixedLeg(builder, fixedLeg):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(fixedLeg), 0)

def AddFixedLeg(builder, fixedLeg):
    VanillaSwapAddFixedLeg(builder, fixedLeg)

def VanillaSwapAddFloatingLeg(builder, floatingLeg):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(floatingLeg), 0)

def AddFloatingLeg(builder, floatingLeg):
    VanillaSwapAddFloatingLeg(builder, floatingLeg)

def VanillaSwapEnd(builder):
    return builder.EndObject()

def End(builder):
    return VanillaSwapEnd(builder)

try:
    from typing import Optional
except:
    pass

class VanillaSwapT(object):

    # VanillaSwapT
    def __init__(self):
        self.swapType = 0  # type: int
        self.fixedLeg = None  # type: Optional[SwapFixedLegT]
        self.floatingLeg = None  # type: Optional[SwapFloatingLegT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        vanillaSwap = VanillaSwap()
        vanillaSwap.Init(buf, pos)
        return cls.InitFromObj(vanillaSwap)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, vanillaSwap):
        x = VanillaSwapT()
        x._UnPack(vanillaSwap)
        return x

    # VanillaSwapT
    def _UnPack(self, vanillaSwap):
        if vanillaSwap is None:
            return
        self.swapType = vanillaSwap.SwapType()
        if vanillaSwap.FixedLeg() is not None:
            self.fixedLeg = SwapFixedLegT.InitFromObj(vanillaSwap.FixedLeg())
        if vanillaSwap.FloatingLeg() is not None:
            self.floatingLeg = SwapFloatingLegT.InitFromObj(vanillaSwap.FloatingLeg())

    # VanillaSwapT
    def Pack(self, builder):
        if self.fixedLeg is not None:
            fixedLeg = self.fixedLeg.Pack(builder)
        if self.floatingLeg is not None:
            floatingLeg = self.floatingLeg.Pack(builder)
        VanillaSwapStart(builder)
        VanillaSwapAddSwapType(builder, self.swapType)
        if self.fixedLeg is not None:
            VanillaSwapAddFixedLeg(builder, fixedLeg)
        if self.floatingLeg is not None:
            VanillaSwapAddFloatingLeg(builder, floatingLeg)
        vanillaSwap = VanillaSwapEnd(builder)
        return vanillaSwap
