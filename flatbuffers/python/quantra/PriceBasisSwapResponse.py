# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PriceBasisSwapResponse(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PriceBasisSwapResponse()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPriceBasisSwapResponse(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PriceBasisSwapResponse
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PriceBasisSwapResponse
    def Swaps(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from quantra.BasisSwapResponse import BasisSwapResponse
            obj = BasisSwapResponse()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PriceBasisSwapResponse
    def SwapsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PriceBasisSwapResponse
    def SwapsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def PriceBasisSwapResponseStart(builder):
    builder.StartObject(1)

def Start(builder):
    PriceBasisSwapResponseStart(builder)

def PriceBasisSwapResponseAddSwaps(builder, swaps):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(swaps), 0)

def AddSwaps(builder, swaps):
    PriceBasisSwapResponseAddSwaps(builder, swaps)

def PriceBasisSwapResponseStartSwapsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSwapsVector(builder, numElems):
    return PriceBasisSwapResponseStartSwapsVector(builder, numElems)

def PriceBasisSwapResponseEnd(builder):
    return builder.EndObject()

def End(builder):
    return PriceBasisSwapResponseEnd(builder)

try:
    from typing import List
except:
    pass

class PriceBasisSwapResponseT(object):

    # PriceBasisSwapResponseT
    def __init__(self):
        self.swaps = None  # type: List[BasisSwapResponseT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        priceBasisSwapResponse = PriceBasisSwapResponse()
        priceBasisSwapResponse.Init(buf, pos)
        return cls.InitFromObj(priceBasisSwapResponse)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, priceBasisSwapResponse):
        x = PriceBasisSwapResponseT()
        x._UnPack(priceBasisSwapResponse)
        return x

    # PriceBasisSwapResponseT
    def _UnPack(self, priceBasisSwapResponse):
        if priceBasisSwapResponse is None:
            return
        if not priceBasisSwapResponse.SwapsIsNone():
            self.swaps = []
            for i in range(priceBasisSwapResponse.SwapsLength()):
                if priceBasisSwapResponse.Swaps(i) is None:
                    self.swaps.append(None)
                else:
                    basisSwapResponse_ = BasisSwapResponseT.InitFromObj(priceBasisSwapResponse.Swaps(i))
                    self.swaps.append(basisSwapResponse_)

    # PriceBasisSwapResponseT
    def Pack(self, builder):
        if self.swaps is not None:
            swapslist = []
            for i in range(len(self.swaps)):
                swapslist.append(self.swaps[i].Pack(builder))
            PriceBasisSwapResponseStartSwapsVector(builder, len(self.swaps))
            for i in reversed(range(len(self.swaps))):
                builder.PrependUOffsetTRelative(swapslist[i])
            swaps = builder.EndVector()
        PriceBasisSwapResponseStart(builder)
        if self.swaps is not None:
            PriceBasisSwapResponseAddSwaps(builder, swaps)
        priceBasisSwapResponse = PriceBasisSwapResponseEnd(builder)
        return priceBasisSwapResponse
