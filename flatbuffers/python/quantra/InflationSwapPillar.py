# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Pillar quote for inflation curve construction.
class InflationSwapPillar(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = InflationSwapPillar()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsInflationSwapPillar(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # InflationSwapPillar
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # InflationSwapPillar
    def Maturity(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from quantra.Period import Period
            obj = Period()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Optional: resolve quote from pricing.quotes (QuoteType=Curve).
    # InflationSwapPillar
    def QuoteId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Inline quote value used when quote_id is empty.
    # InflationSwapPillar
    def QuoteValue(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def InflationSwapPillarStart(builder):
    builder.StartObject(3)

def Start(builder):
    InflationSwapPillarStart(builder)

def InflationSwapPillarAddMaturity(builder, maturity):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(maturity), 0)

def AddMaturity(builder, maturity):
    InflationSwapPillarAddMaturity(builder, maturity)

def InflationSwapPillarAddQuoteId(builder, quoteId):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(quoteId), 0)

def AddQuoteId(builder, quoteId):
    InflationSwapPillarAddQuoteId(builder, quoteId)

def InflationSwapPillarAddQuoteValue(builder, quoteValue):
    builder.PrependFloat64Slot(2, quoteValue, 0.0)

def AddQuoteValue(builder, quoteValue):
    InflationSwapPillarAddQuoteValue(builder, quoteValue)

def InflationSwapPillarEnd(builder):
    return builder.EndObject()

def End(builder):
    return InflationSwapPillarEnd(builder)

try:
    from typing import Optional
except:
    pass

class InflationSwapPillarT(object):

    # InflationSwapPillarT
    def __init__(self):
        self.maturity = None  # type: Optional[PeriodT]
        self.quoteId = None  # type: str
        self.quoteValue = 0.0  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        inflationSwapPillar = InflationSwapPillar()
        inflationSwapPillar.Init(buf, pos)
        return cls.InitFromObj(inflationSwapPillar)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, inflationSwapPillar):
        x = InflationSwapPillarT()
        x._UnPack(inflationSwapPillar)
        return x

    # InflationSwapPillarT
    def _UnPack(self, inflationSwapPillar):
        if inflationSwapPillar is None:
            return
        if inflationSwapPillar.Maturity() is not None:
            self.maturity = PeriodT.InitFromObj(inflationSwapPillar.Maturity())
        self.quoteId = inflationSwapPillar.QuoteId()
        self.quoteValue = inflationSwapPillar.QuoteValue()

    # InflationSwapPillarT
    def Pack(self, builder):
        if self.maturity is not None:
            maturity = self.maturity.Pack(builder)
        if self.quoteId is not None:
            quoteId = builder.CreateString(self.quoteId)
        InflationSwapPillarStart(builder)
        if self.maturity is not None:
            InflationSwapPillarAddMaturity(builder, maturity)
        if self.quoteId is not None:
            InflationSwapPillarAddQuoteId(builder, quoteId)
        InflationSwapPillarAddQuoteValue(builder, self.quoteValue)
        inflationSwapPillar = InflationSwapPillarEnd(builder)
        return inflationSwapPillar
