# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Optional Asian feature (discrete averaging).
class EquityAsianFeature(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EquityAsianFeature()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEquityAsianFeature(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # EquityAsianFeature
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # EquityAsianFeature
    def AverageType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # EquityAsianFeature
    def FixingDates(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # EquityAsianFeature
    def FixingDatesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # EquityAsianFeature
    def FixingDatesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # EquityAsianFeature
    def RunningAccumulator(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # EquityAsianFeature
    def PastFixings(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

def EquityAsianFeatureStart(builder):
    builder.StartObject(4)

def Start(builder):
    EquityAsianFeatureStart(builder)

def EquityAsianFeatureAddAverageType(builder, averageType):
    builder.PrependInt8Slot(0, averageType, 0)

def AddAverageType(builder, averageType):
    EquityAsianFeatureAddAverageType(builder, averageType)

def EquityAsianFeatureAddFixingDates(builder, fixingDates):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(fixingDates), 0)

def AddFixingDates(builder, fixingDates):
    EquityAsianFeatureAddFixingDates(builder, fixingDates)

def EquityAsianFeatureStartFixingDatesVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartFixingDatesVector(builder, numElems):
    return EquityAsianFeatureStartFixingDatesVector(builder, numElems)

def EquityAsianFeatureAddRunningAccumulator(builder, runningAccumulator):
    builder.PrependFloat64Slot(2, runningAccumulator, 0.0)

def AddRunningAccumulator(builder, runningAccumulator):
    EquityAsianFeatureAddRunningAccumulator(builder, runningAccumulator)

def EquityAsianFeatureAddPastFixings(builder, pastFixings):
    builder.PrependInt32Slot(3, pastFixings, 0)

def AddPastFixings(builder, pastFixings):
    EquityAsianFeatureAddPastFixings(builder, pastFixings)

def EquityAsianFeatureEnd(builder):
    return builder.EndObject()

def End(builder):
    return EquityAsianFeatureEnd(builder)

try:
    from typing import List
except:
    pass

class EquityAsianFeatureT(object):

    # EquityAsianFeatureT
    def __init__(self):
        self.averageType = 0  # type: int
        self.fixingDates = None  # type: List[str]
        self.runningAccumulator = 0.0  # type: float
        self.pastFixings = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        equityAsianFeature = EquityAsianFeature()
        equityAsianFeature.Init(buf, pos)
        return cls.InitFromObj(equityAsianFeature)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, equityAsianFeature):
        x = EquityAsianFeatureT()
        x._UnPack(equityAsianFeature)
        return x

    # EquityAsianFeatureT
    def _UnPack(self, equityAsianFeature):
        if equityAsianFeature is None:
            return
        self.averageType = equityAsianFeature.AverageType()
        if not equityAsianFeature.FixingDatesIsNone():
            self.fixingDates = []
            for i in range(equityAsianFeature.FixingDatesLength()):
                self.fixingDates.append(equityAsianFeature.FixingDates(i))
        self.runningAccumulator = equityAsianFeature.RunningAccumulator()
        self.pastFixings = equityAsianFeature.PastFixings()

    # EquityAsianFeatureT
    def Pack(self, builder):
        if self.fixingDates is not None:
            fixingDateslist = []
            for i in range(len(self.fixingDates)):
                fixingDateslist.append(builder.CreateString(self.fixingDates[i]))
            EquityAsianFeatureStartFixingDatesVector(builder, len(self.fixingDates))
            for i in reversed(range(len(self.fixingDates))):
                builder.PrependUOffsetTRelative(fixingDateslist[i])
            fixingDates = builder.EndVector()
        EquityAsianFeatureStart(builder)
        EquityAsianFeatureAddAverageType(builder, self.averageType)
        if self.fixingDates is not None:
            EquityAsianFeatureAddFixingDates(builder, fixingDates)
        EquityAsianFeatureAddRunningAccumulator(builder, self.runningAccumulator)
        EquityAsianFeatureAddPastFixings(builder, self.pastFixings)
        equityAsianFeature = EquityAsianFeatureEnd(builder)
        return equityAsianFeature
