# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Overnight index fully described by conventions
class OvernightIndexSpec(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OvernightIndexSpec()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOvernightIndexSpec(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # OvernightIndexSpec
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # OvernightIndexSpec
    def Name(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # OvernightIndexSpec
    def FixingDays(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # OvernightIndexSpec
    def Calendar(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 38

    # OvernightIndexSpec
    def DayCounter(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

def OvernightIndexSpecStart(builder):
    builder.StartObject(4)

def Start(builder):
    OvernightIndexSpecStart(builder)

def OvernightIndexSpecAddName(builder, name):
    builder.PrependInt8Slot(0, name, 0)

def AddName(builder, name):
    OvernightIndexSpecAddName(builder, name)

def OvernightIndexSpecAddFixingDays(builder, fixingDays):
    builder.PrependInt32Slot(1, fixingDays, 0)

def AddFixingDays(builder, fixingDays):
    OvernightIndexSpecAddFixingDays(builder, fixingDays)

def OvernightIndexSpecAddCalendar(builder, calendar):
    builder.PrependInt8Slot(2, calendar, 38)

def AddCalendar(builder, calendar):
    OvernightIndexSpecAddCalendar(builder, calendar)

def OvernightIndexSpecAddDayCounter(builder, dayCounter):
    builder.PrependInt8Slot(3, dayCounter, 0)

def AddDayCounter(builder, dayCounter):
    OvernightIndexSpecAddDayCounter(builder, dayCounter)

def OvernightIndexSpecEnd(builder):
    return builder.EndObject()

def End(builder):
    return OvernightIndexSpecEnd(builder)


class OvernightIndexSpecT(object):

    # OvernightIndexSpecT
    def __init__(self):
        self.name = 0  # type: int
        self.fixingDays = 0  # type: int
        self.calendar = 38  # type: int
        self.dayCounter = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        overnightIndexSpec = OvernightIndexSpec()
        overnightIndexSpec.Init(buf, pos)
        return cls.InitFromObj(overnightIndexSpec)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, overnightIndexSpec):
        x = OvernightIndexSpecT()
        x._UnPack(overnightIndexSpec)
        return x

    # OvernightIndexSpecT
    def _UnPack(self, overnightIndexSpec):
        if overnightIndexSpec is None:
            return
        self.name = overnightIndexSpec.Name()
        self.fixingDays = overnightIndexSpec.FixingDays()
        self.calendar = overnightIndexSpec.Calendar()
        self.dayCounter = overnightIndexSpec.DayCounter()

    # OvernightIndexSpecT
    def Pack(self, builder):
        OvernightIndexSpecStart(builder)
        OvernightIndexSpecAddName(builder, self.name)
        OvernightIndexSpecAddFixingDays(builder, self.fixingDays)
        OvernightIndexSpecAddCalendar(builder, self.calendar)
        OvernightIndexSpecAddDayCounter(builder, self.dayCounter)
        overnightIndexSpec = OvernightIndexSpecEnd(builder)
        return overnightIndexSpec
