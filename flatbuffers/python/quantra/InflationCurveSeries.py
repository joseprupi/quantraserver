# automatically generated by the FlatBuffers compiler, do not modify

# namespace: quantra

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# One sampled inflation series aligned to grid_dates.
class InflationCurveSeries(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = InflationCurveSeries()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsInflationCurveSeries(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # InflationCurveSeries
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # InflationCurveSeries
    def Measure(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # InflationCurveSeries
    def Values(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # InflationCurveSeries
    def ValuesAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # InflationCurveSeries
    def ValuesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # InflationCurveSeries
    def ValuesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def InflationCurveSeriesStart(builder):
    builder.StartObject(2)

def Start(builder):
    InflationCurveSeriesStart(builder)

def InflationCurveSeriesAddMeasure(builder, measure):
    builder.PrependInt8Slot(0, measure, 0)

def AddMeasure(builder, measure):
    InflationCurveSeriesAddMeasure(builder, measure)

def InflationCurveSeriesAddValues(builder, values):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(values), 0)

def AddValues(builder, values):
    InflationCurveSeriesAddValues(builder, values)

def InflationCurveSeriesStartValuesVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartValuesVector(builder, numElems):
    return InflationCurveSeriesStartValuesVector(builder, numElems)

def InflationCurveSeriesEnd(builder):
    return builder.EndObject()

def End(builder):
    return InflationCurveSeriesEnd(builder)

try:
    from typing import List
except:
    pass

class InflationCurveSeriesT(object):

    # InflationCurveSeriesT
    def __init__(self):
        self.measure = 0  # type: int
        self.values = None  # type: List[float]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        inflationCurveSeries = InflationCurveSeries()
        inflationCurveSeries.Init(buf, pos)
        return cls.InitFromObj(inflationCurveSeries)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, inflationCurveSeries):
        x = InflationCurveSeriesT()
        x._UnPack(inflationCurveSeries)
        return x

    # InflationCurveSeriesT
    def _UnPack(self, inflationCurveSeries):
        if inflationCurveSeries is None:
            return
        self.measure = inflationCurveSeries.Measure()
        if not inflationCurveSeries.ValuesIsNone():
            if np is None:
                self.values = []
                for i in range(inflationCurveSeries.ValuesLength()):
                    self.values.append(inflationCurveSeries.Values(i))
            else:
                self.values = inflationCurveSeries.ValuesAsNumpy()

    # InflationCurveSeriesT
    def Pack(self, builder):
        if self.values is not None:
            if np is not None and type(self.values) is np.ndarray:
                values = builder.CreateNumpyVector(self.values)
            else:
                InflationCurveSeriesStartValuesVector(builder, len(self.values))
                for i in reversed(range(len(self.values))):
                    builder.PrependFloat64(self.values[i])
                values = builder.EndVector()
        InflationCurveSeriesStart(builder)
        InflationCurveSeriesAddMeasure(builder, self.measure)
        if self.values is not None:
            InflationCurveSeriesAddValues(builder, values)
        inflationCurveSeries = InflationCurveSeriesEnd(builder)
        return inflationCurveSeries
