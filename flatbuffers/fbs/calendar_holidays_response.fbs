include "enums.fbs";

namespace quantra;

/// Holidays/vacations for a calendar date range.
table CalendarHolidaysResponse {
    calendar:enums.Calendar = TARGET;
    start_date:string;
    end_date:string;
    dates:[string] (required);
    count:uint;
}

root_type CalendarHolidaysResponse;
