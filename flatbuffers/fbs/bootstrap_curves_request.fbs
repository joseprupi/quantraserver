include "common.fbs";
include "pricing.fbs";
include "curve_query.fbs";

namespace quantra;

/// Request to bootstrap curves from Pricing and sample them by query.
table BootstrapCurvesRequest {
    pricing:Pricing (required);
    queries:[CurveQuerySpec] (required);
}

root_type BootstrapCurvesRequest;
